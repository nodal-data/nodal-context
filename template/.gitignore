# Live-verification run traces are generated output, not context — keep them local.
evals/runs/*
!evals/runs/.gitkeep

# Transient dbt bootstrap summary (Stage 0) — not ACF, discard after drafting.
.dbt-findings.json

# macOS
.DS_Store
