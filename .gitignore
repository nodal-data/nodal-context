# Scratch / local-only files
FINDINGS.md
.nodal.local.json

# Simulated-analyst briefs — local only; may contain customer-confidential data.
# (Inline comments are NOT allowed on gitignore pattern lines — keep them above.)
responses*.md
RESPONSES.md
shorelane.context.md
3y.context.md

# macOS
.DS_Store

# Local clean-room operator wrappers (the reusable harness is tracked under
# scripts/integration/).
clean-test.sh
clean-test.local.sh

# Python
__pycache__/
*.pyc

# Generated context output + dbt bootstrap artifact (if written inside the clone)
analytics-context/
.dbt-findings.json

# Transient query-history mining artifacts (Stage 0) — raw warehouse SQL, never committed
.query-history-rows.json
.query-findings.json

.env
.envrc
toolbox 

.vscode/

evals/captures/*
!evals/captures/.gitkeep

# Dedicated Chrome profile for dashboard-verify, if a user relocates it into the
# repo (the optional setup-nodal browser template defaults to a home-directory profile).
# Profiles hold session cookies; never commit one.
.nodal-dashboard-verify-profile/

query-upgrade-2026-07-15.md
